apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.appName }}-cluster-ip-service"
spec:
  selector:
    app: {{ .Values.appName }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP
