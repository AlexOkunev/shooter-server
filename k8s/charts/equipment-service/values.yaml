appName: shooter-server-equipment-grpc

replicas: 1
terminationGracePeriodSeconds: 30

image:
  repository: otus-java-advanced/shooter-server-equipment-grpc-server
  tag: "1.0"
  pullPolicy: IfNotPresent

grpc:
  port: 6565
  shutdownGrace: 20
  enableReflection: true

actuator:
  port: 9090

db:
  name: equipment_db
  serviceName: equipment-db-service
  secretName: equipment-db-secret
  port: 5434
  schema: public
  jpa:
    jdbcBatchSize: 100
    jdbcFetchSize: 400

resources:
  requests:
    cpu: "1"
    memory: "1Gi"
  limits:
    cpu: "2"
    memory: "2Gi"