apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.appName }}-cluster-ip-service"
spec:
  selector:
    app: {{ .Values.appName }}
  ports:
    - name: http
      port: {{ .Values.grpc.port }}
      targetPort: {{ .Values.grpc.port }}
  type: ClusterIP
